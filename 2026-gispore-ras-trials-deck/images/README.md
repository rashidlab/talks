# Images needed for talk.qmd

The deck references the images below. Until they are supplied, Quarto will
render the slide with the image's alt-text shown in a placeholder box —
the rest of the slide still renders correctly.

| File | Slide | Description |
|---|---|---|
| `rasolute302-km.png` | 3 | KM curve from RASolute 302 (OS, 13.2 vs 6.7 mo) |
| `ras-rtk-feedback.svg` | 5 | Schematic of RAS(ON) inhibition driving RTK upregulation |
| `dose-response-trio.svg` | 9 | Three side-by-side dose-response curves (monotone, plateau, umbrella) |
| `uboin-utility-heatmap.svg` | 12 | Dose × (tox, eff) plane with utility contours, OBD region marked |
| `tumor-evolution-cartoon.svg` | 15 | Tumor under therapy with subclones expanding |
| `baton-acquisition-cartoon.svg` | 22 | 2D acquisition surface, feasible region shaded, BATON converging |

## Optional / removed

The build spec also listed `fda-project-optimus-cover.png` (slide 8),
`pangea-dose-response-hypothesis.svg` (slide 13), `resistance-two-classes.svg`
(slide 16), `anytime-valid-timeline.svg` (slide 19), and
`parameter-space-cartoon.svg` (slide 21). In the rendered deck these
slides carry their content in text and a callout instead — adding a
figure to each one would crowd them in the time available. They can be
reintroduced if a dry-run shows pacing room.
